# Real Estate Listing Platform

Browse approved listings with **city/price filters**, register as an **agent** to post properties (admin approve/reject), and **message** owners about listings.

**Stack:** Python · Flask · SQLite · Jinja2 · HTML/CSS · Session auth · Image uploads

**Run:** `pip install flask` → `python app.py` → open http://127.0.0.1:5000
